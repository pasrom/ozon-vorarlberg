# Derived data — produced by the cron job, published on the data branch
data.json
archive.json
history.jsonl
*.tmp
_data/
_site/
demo/
logs/
.venv/

# Raw EEA parquet cache (~26 MB). mini/install.sh rebuilds it.
cache/

# Python
__pycache__/
*.pyc

# Personal session notes. Deliberately excluded: these are your own records,
# not a project file. To publish them anyway, delete this line and commit —
# the reverse is not nearly as easy.
ZUSAMMENFASSUNG.md
